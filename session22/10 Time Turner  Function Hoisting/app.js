turnBackTime();

function turnBackTime() {
    console.log("Going back in time!");
}
console.log("Function declaration allows us to call it before defining it, due to the hoisting mechanism.This happens beacause JavaScript hoisting mechanism lifts function declaration to the top of the script before execution");
